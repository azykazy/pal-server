# このファイルを terraform.tfvars にコピーして値を埋める (terraform.tfvars は gitignore 済み)
# パスワード類はここには書かず、apply 後に `mise run seed-secrets` で Key Vault に投入する。

# パルワールド専用アカウントのサブスクリプション ID (`az account show --query id -o tsv`)
subscription_id = "00000000-0000-0000-0000-000000000000"

# SSH 公開鍵の中身 (例: `cat ~/.ssh/palworld_azure.pub` の出力)
ssh_public_key = "ssh-ed25519 AAAA... palworld-azure"

# SSH を許可する CIDR (自宅のグローバル IP)。空文字なら SSH 非開放 (通常運用では不要)
allowed_ssh_cidr = ""

server_name = "My Palworld Server"

# Discord アプリケーション情報 (docs/SETUP.md 参照。どちらも公開情報なので tfvars に書いてよい)
discord_public_key     = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
discord_application_id = "123456789012345678"
